<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
<style>
    *{
        margin: 0 ;
        padding: 0 ;
        box-sizing: border-box;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }
    body{
        height: 100dvh;
        width: 100dvw;
        display: flex;
        flex-direction: column;
    }
    nav {
        display: flex;
        justify-content: center;
    }
    nav li{
        list-style: none;
    }
    nav :is(a,button){
        text-decoration: none;
        padding: 10px 30px;
        font-size: large;
        font-weight: bold;
        border: none;
        background-color: transparent;
        display: inline-block;
        cursor: pointer;
        color: darkblue;
    }
    nav li:not(:last-child)::after{
        content: "|";
    }
    nav :is(a,button):hover{
        color: gold;
    }
      h1 {
        text-align: center;
      }
      :is(form.login-form ,form.new-user-form) {
        width: 350px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
      }
      :is(form.login-form ,form.new-user-form) :is(input, button) {
        padding: 3px 2px;
        font-size: large;
        border-radius: 10px;
      }
      footer{
        text-align: center;
        margin: auto 0 0 0;
      }
</style>
  </head>
  <body>
    <header>
        <nav>
            <li><a href="/">Home</a></li>
            <li><a href="/inbox">Inbox</a></li>
            <li><a href="/users">User</a></li>


<% if(!!(loggedInUser&&loggedInUser.email)){ %>

  <li><button onclick="handleLogout()">Log out</button></li>
  
  <% }else{ %>
    
    <li><a href="/login">Login</a></li>

<% } %>

          </nav>
    </header>
<script>
  const handleLogout=()=>{
    fetch('/logout',{
      method:"DELETE"
    }).then((res)=>res.json()).then(data=>console.log(data))
  }
</script>